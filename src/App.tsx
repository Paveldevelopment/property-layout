import React from "react";

import Layout from "./components/Layout";

const App: React.FC = () => {
  return (
    <div className="app-container">
      <Layout />
    </div>
  );
};

export default App;
